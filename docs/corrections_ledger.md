# Corrections ledger (B1--B84)

Every finding from the pre-revision workspace audit is registered here with its evidence, its disposition, and the manuscript location it affects. The consolidated twenty-entry corrections section of the Online Resource cites these ledger ids.

**Status vocabulary**: `fixed-P1` corrected during revision phase 1; `pending-P2`..`pending-P6` scheduled; `decision-needed` required an author decision; `wont-fix` deliberate, reason recorded; `annotated` recorded as a known property, no code change.

84 findings, B1--B84 (B48--B56 of one interim report were renumbered B57--B65; see the note below that section).

---

## Tier 1 — paper/implementation contradictions (highest priority)

| ID | Problem | Evidence | Disposition | Manuscript locations affected |
|---|---|---|---|---|
| **B1** | ESM §S3.5 SNI hyperparameters contradict every manifest: published `H=4`, hidden 64, `lr=1e-3`, `rho=0.2` vs actual `num_heads=16`, `hidden_dims=[256,128,64]`, `emb_dim=128`, `lr=2e-4`, `mask_fraction=0.15` | `ESM:242-243` vs `data/manifest_sni_v03_main.csv` (60/60 rows identical); corroborated by 16 head columns in all `lambda_per_head.csv` and `n_heads=16` in all 670 rows of `major3_lambda_summary.csv` | **fixed-P1 (config side)** — `configs/methods.yaml` now holds the true values and the ESM table will be generated from it (E1+E5). Whether to keep the true values or re-run at the published ones is `decision-needed` (Q1-1) | ESM §S3.5 `:240-245` |
| **B2** | ESM Table S6 states "All experiments ran on CPU (Intel Xeon, 32 GB RAM)"; SNI (410 runs) and TabCSDI (60 runs) ran on an RTX A6000, on an i9-13900K with 123 GiB RAM | `ESM:530` vs `use_gpu=True` in all SNI manifests, `use_gpu=True` in all 60 TabCSDI rows of `agg_baselines_new/summary_all.csv`, and `results_all/environment_snapshot.txt` | **decision-needed** (Q1-2): correct the hardware statement plus per-method device footnotes, or re-time everything on one uniform device. `configs/methods.yaml:device` records the R0 assignment either way | ESM Table S6 `:504-532`, esp. `:530`; main `:511` |
| **B3** | Fig. 3 (multi-rate sensitivity): under MCAR and MAR every non-SNI method has only the 30 % point, yet the text claims a trend comparable to MissForest and MIWAE. Not disclosed | `outputs_paper/csv/multirate_sensitivity_data.csv` coverage matrix; `manifest_baselines_rate_sweep_missforest.csv` (120 rows) has no result directory | **pending-P2** — covered by the unified re-run (D1) | main `:250-253` (figure), `:291` (claim) |
| **B4** | 410/410 SNI runs flagged `did_not_converge`, always at `G=3`; the `eps=1e-4` early stop never fires because `imputer.py:350` omits `cat_vars`, making the categorical branch at `:676-679` dead code. ESM `:537` attributes runtime differences to "EM iterations before convergence", which is constant | `agg_sni_*/summary_all.csv` `warning` column non-null in 410/410; `convergence_iterations` = 3 in all | **P1: port as-is + read-only diagnostic** (T1.3.2). Changing the criterion is a P3 decision | main algorithm `:155`, `:191-197`; ESM `:134`, `:242-244`, `:537` |
| **B5** | NHANES downstream imputes the full table before splitting → test-set leakage (reviewer R1-3) | `exp2_downstream_task_validation.py:391-404` → `:460`/`:477` → `:517-520` | **fixed-P1 (protocol side)**, re-run **pending-P2** | main Table 5 Panel B `:433-440`; result claim `:460` |
| **B6** | Every baseline receives the ground-truth complete table and MeanMode/MICE/KNN/TabCSDI use its statistics; all call `fallback_fillna(X_imp, X_complete, …)`. SNI alone never touches it | `registry.py:64-67, 80-83, 114-118, 150-155, 193-198, 249-254, 280-284, 328-332`; `baselines/utils.py:53-87` | **fixed-P1** (T1.4) — new `impute(X_missing, schema)` interface; impact quantified | affects main Tables 1, 5 and ESM Table S5 |
| **B7** | Table 5 Panel A: KNN/MICE/GAIN/MIWAE were never run. Paper says "omitted for table width"; ESM says "successfully processed subset" — both false | `run_all_experiments.py:427` `--imputers` list; `per_seed_metrics.csv` has exactly 50 rows = 5 imputers × 5 seeds × 2 models with no failures and no logs | **pending-P2** (run them) + **fixed-P5** (rewrite both notes) | main `:445-447`; ESM `:237` |
| **B11** | Fig. 4b: paper says SHAP came from "an auxiliary gradient-boosting predictor"; the code uses `RandomForestClassifier/Regressor` | `exp4_shap_comparison.py:225, 248` vs main `:336` | **pending-P3** (R2-1 rebuild will settle the surrogate); text fix **pending-P5** | main `:336`, Fig. 4 caption `:345` |
| **B32** | MAR driver differs between main experiments (`ID`) and Ext1/Ext2 (real covariates) | 18 MAR `meta.json` vs `readme_CL.txt:339, 387, 424, 441`; `ext1/exp2:339-349` fallback prefers `fairness_col` | **decision-needed** (Q1-3), implement **pending-P2** | main `:239`; ESM `:213-216`, `:583` |
| **B33** | README dataset sizes wrong: claims MIMIC 5,000×8, eICU 5,000×9, NHANES 7,500×9, ComCri 2,000×8; actual 2052×8, 1430×20, 2274×12, 1994×10 | measured; `*_meta.json` `n_rows`/`n_cols` agree with the measurement | **fixed-P1** — `configs/datasets.yaml` carries measured values; new README generated from it | R0 `README.md:78-87` (repo only, not the manuscript) |
| **B17** | Proxy-injection weights (0.123, 0.179) fall below the script's own alert threshold of 0.6, yet the paper calls them "elevated reliance weight" | `exp1_audit_story_leakage.py:262-264` vs main `:373` | **pending-P3** | main Table 4 `:377-405`, `:373` |
| **B48** ⚠️ NEW | **SNI results are not reproducible from a fixed seed, on any device.** The per-feature seed is `self.cfg.seed + (hash(f) % 10000)`, and CPython randomises `str.__hash__` per process. `set_global_seed` sets `os.environ["PYTHONHASHSEED"]` *inside* the running interpreter (`utils.py:31`), which has no effect — that variable is read only at interpreter start-up. Measured: three fresh processes gave offsets 2182, 5364, 8415 for `hash("RESP")`. The environment snapshot does not record `PYTHONHASHSEED`, so R0 almost certainly ran unseeded in this respect | `sni/utils.py:31`; `sni/imputer.py:753` (continuous) and `:991` (categorical); measured across three processes | **do-not-touch in P1** — this is class A behavior. Reported, quantified in the T1.3 equivalence check, **decision-needed** (Q1-8) | main `:197` and ESM §S4 claim seed-based reproducibility; ESM `:248-266` reproducibility notes |
| **B10-refined** | The R0 GPU non-determinism is narrower than P0 stated. `enable_performance_mode()` ran once at the top of `impute()`, but every per-feature `set_global_seed` call (`:753`, `:991`) re-asserts `cudnn.deterministic=True, benchmark=False`. So during actual training only **TF32 remained enabled**; cudnn benchmark autotuning did not | `utils.py:28-29` inside `set_global_seed`; call sites `imputer.py:753, :991` inside the EM loop | **fixed-P1** — `common/determinism.py` separates the modes; documented accurately | ESM §S4 |

> **Record moved here from `configs/methods.yaml` (P3-A ruling 1, 2026-08-20).** R0's de-facto device assignment, formerly a `device:` block in `methods.yaml`, was: default CPU; SNI and TabCSDI `gpu_if_available` (both in fact ran on the RTX A6000 — see B2); GAIN and MIWAE explicitly CPU. The block was removed from the config because a historical record inside a live config file is the B1 pattern (declared ≠ implemented) waiting to recur: the W4 ESM generator reads `methods.yaml`, and had it sourced devices there, the ESM would have printed `SNI: gpu_if_available` while the main text says CPU. The binding placement is `configs/scheduling.yaml: method_placement` (ruling A-2); `tests/test_adjudications.py` now asserts both that `methods.yaml` carries no device key and that the ESM runtime table's device column comes from `scheduling.yaml`.

---

## Tier 2 — scientific-validity defects

| ID | Problem | Evidence | Disposition | Affected |
|---|---|---|---|---|
| **R1-4 / MAR** | `_mar_propensity` broadcasts one row-propensity vector to every column (`np.repeat`), and all 18 MAR masks use `ID` (a row index) as the driver. Row-index correlation +0.67…+0.80 | `missing_data_generator.py:467`; 18 `meta.json`; measured correlations | **fixed-P1 (simulator)** T1.5; new masks **pending-P2** after driver decision | main `:239`; ESM `:213-216` |
| **B34** | Target leakage: eICU `composite_risk_score` and NHANES `metabolic_score` (a deterministic threshold function of five features) are used as imputation features | `datasets.yaml:147, 232`; upstream dictionaries mark both as targets | **annotated-P1**, quantified in T1.6a, **decision-needed** (Q1-4) | main Tables 1, 2, 5; ESM Table S5 |
| **B35** | eICU has two zero-variance columns (`hours_since_admission`≡24, `vasopressor_use_std`≡1.0) counted in RMSE | measured `nunique()==1`; `datasets.yaml:155, 167` | **annotated-P1**, quantified in T1.6a, **decision-needed** (Q1-5) | main Table 1; Table 3 "d=20"; ESM Fig. S7 |
| **B37** | eICU complete table is a 28.6 % complete-case subsample (5000→1430); `vasopressor_dose` was 55.86 % missing upstream | measured column-wise missingness of `eICU_composite_risk_score_v2_strict.csv` | **pending-P5** (disclose in Methods) | ESM §S3.1 `:203-208` |
| **B36** | The MIMIC derived table has no build script anywhere on the machine | `~/data_MIMIC_cong/` contains CSVs only; `mimic_extract_v7.py` produces a different 31-column table | **decision-needed** (Q1-6) — evaluated in T1.6c | ESM §S3.1; reproducibility statement `:600-605` |
| **B41** | Redundant columns: `age_band`/`age` (NHANES), `age_band`/`age_years` (eICU) are deterministic functions of each other | `datasets.yaml:221, 229` and `:152, 168` | **annotated-P1**, quantified in T1.6a | ESM Table S1 |
| **B42** | `metabolic_score` (6 integer levels) declared continuous while eICU's `composite_risk_score` (14 levels) is declared categorical | `datasets.yaml:232` vs `:147` | **annotated-P1**, quantified in T1.6a | main Table 1; ESM Table S1 |
| **B47** | Unit mixture / uncleaned values: eICU `vasopressor_dose` spans 0.008–6250, `urine_output_min` max 220 912; NHANES `diastolic_bp` min 0.0 | measured min/max | **annotated-P1**, quantified in T1.6a | ESM §S3.1 |
| **B39** | MNAR gives `ALARM` a 37.8 % actual rate against a 30 % target — and `ALARM` is the ESM Table S9 evaluation target | `MIMIC_MNAR_30per_meta.json` `per_column_missing_rate` | **fixed-P1** (per-column rate calibration option in the new simulator) | ESM Table S9 |
| **B46** | eICU `vasopressor_use_std` has one distinct value, so MNAR's `n_high = max(1, …) = 1` makes every row "high", degenerating to 1.5×MCAR | `missing_data_generator.py:535-541` | **fixed-P1** (T1.5) | — |
| **B45** | MNAR's categorical RNG is shared with `_enforce_min_missing_per_column` and `calibrate_mask_to_rate`, so changing the number or order of categorical columns changes every later random draw | `missing_data_generator.py:617, 316, 383-405` | **fixed-P1** (T1.5, independent RNG streams keyed by column name — deliberately *not* `SeedSequence.spawn`, which is position-dependent and would reintroduce the coupling) | — |
| **B49** ⚠️ NEW | **The released MAR masks at 10 %, 30 % and 50 % are three different mechanisms, not three strengths of one.** The mean-linear rescale `p_row * (rate/mean_p)` (`missing_data_generator.py:460-464`) destroys the logistic form, and the distortion depends on the target rate. Measured log-odds contrast between the driver's top and bottom deciles — a quantity that is constant under a true logistic — is 2.19 at rate 0.1, 2.70 at 0.3 and 4.06 at 0.5, against a constant 4.02–4.11 under intercept fitting. So the 10 % masks carry roughly half the MAR dependence strength of the 50 % masks | measured in T1.5; `missing_data_generator.py:460-464` vs `calibration.solve_intercept` | **fixed-P1** (logit-space bisection, drift < 2.3 %); affects the interpretation of the multi-rate figure | main Fig. 3 `:250-253`, `:291`; ESM §S3.2 `:209-219` |
| **B50** ⚠️ NEW | **The continuous twin of B46.** eICU `hours_since_admission` is constant 24, so the MNAR quantile step function puts the whole column in the lowest bin and gives it `rate × p_low_mult = 0.4 × rate`. Measured actual rates: 4.1 % / 11.5 % / 17.9 % against 10 / 30 / 50 % targets — while `vasopressor_use_std` in the same table got 13.7 % / 43.4 % / 73.1 %, a spread of up to 4.1×. The paper reports a single uniform rate. Overall 16 of 20 eICU columns fall outside a ±1 % tolerance under MNAR 30 % | measured against the R0 masks; `missing_data_generator.py:505-526` | **fixed-P1** (per-column rate calibration; 0/20 columns out of tolerance) | ESM §S3.2; ESM Table S9 |
| **B51** ⚠️ NEW | **Three of the six derived tables are sorted**, so a row-index correlation is a property of the table, not only of the mechanism: AutoMPG is sorted by `model_year` (r = +0.997 with row index), MIMIC by `ALARM` (+0.598) / `ABP` (−0.442), Concrete by `ConcreteCS` (−0.311). A perfectly defensible driver such as `model_year` still yields a mask correlated with row order. This must be stated in the response letter, otherwise the fix to R1-4 looks incomplete | measured per column against row index | **annotated-P1** — `row_order: shuffle` implemented but **off by default**; `meta.json` records `pearson_r_driver_vs_rowindex` so any high value can be attributed on the spot. **decision-needed** (Q1-9) | main `:239`; ESM `:213-216` |
| **B52** ⚠️ NEW | `Concrete.Duration` is declared `continuous` in `configs/datasets.yaml` but R0 actually masked it as **categorical** — `infer_column_types:179-185` applies an `nunique <= 20` heuristic and the column has 14 integer levels. The two readings imply different MNAR mechanisms and different evaluation metrics (Accuracy vs NRMSE) | `Concrete_*_meta.json` records the categorical treatment; config says otherwise | **annotated-P1** — legacy profile pins the R0 reading for bitwise reproduction; **decision-needed** (Q1-10) | ESM Table S1 |
| **B8** | No effect sizes, no confidence intervals, no omnibus test, no equivalence test anywhere | repo-wide grep; abandoned attempt visible at `06_gen_supp_tables.py:293-297` | **fixed-P1** (`code_SNI/stats`, T1.7) | main `:247`; ESM §S6 `:317-354`; Table S3 |
| **B53** ⚠️ NEW | **Table S3's SNI arm is a contaminated pool.** `exp5_significance_tests.py::_load_sni_results` reads `agg_sni_v03_main` (60 runs) *and* `sni_v03_main/*/metrics_summary.csv` (the same 60 runs again, double-counted) *and* `agg_sni_v03_ablation_lambda` (50 fixed-λ runs whose `variant` column also says `SNI`). So for the MIMIC/MAR and NHANES/MAR settings the "SNI" mean is 10 duplicated learned-λ runs plus 25 fixed-λ ablation runs, not 5 learned-λ runs. Proven by reconstruction: simulating "main×2 + ablation×1" reproduces all 32 stored `mean_diff` values to 1e-12, while the correct pooling reproduces 0/32 | `ext2/scripts/exp5_significance_tests.py::_load_sni_results`; unit test `test_r0_table_s3_pooling_defect_is_reproduced_exactly` | **fixed-P1** in the new pipeline (λ-ablation rows carry `experiment_family='lambda_ablation'` and an assertion forbids pooling them with the main grid). **Impact: all 32 significance verdicts are unchanged**; `mean_diff` shifts in the 4th decimal (max \|Δ\| = 0.00099) and 3 of 8 Macro-F1 W statistics move by 1 | ESM Table S3 `:326-354` — the Δ column is not the quantity the caption describes |
| **B54** ⚠️ NEW | `n_settings = 12` is wrong for the categorical metrics. Concrete has no categorical columns, so `cat_Macro-F1` is NaN there, `_wilcoxon_safe` drops it, and the true pair count is **10**. The stored CSV and Table S3 both say 12. p-values are unaffected (the NaN was already dropped) but any effect size back-derived from the stored `W` comes out too small, because `T = n(n+1)/2` is computed as 78 instead of 55 | `exp5_significance_tests.py`; corrected in `effect_size.R0_EFFECTIVE_N` | **fixed-P1** | ESM Table S3 caption |
| **B55** ⚠️ NEW | Four cells have `mean_diff` and `median_diff` of **opposite sign**, all with p > 0.5: NRMSE vs HyperImpute (−0.0060 vs +0.0031), Spearman vs HyperImpute (−0.0457 vs +0.0132), Spearman vs TabCSDI (−0.0325 vs +0.0141), Macro-F1 vs HyperImpute (+0.0003 vs −0.0090). The difference vectors are skewed and a few settings dominate the mean. Directly relevant to R1-5: Table S3's "SNI vs HyperImpute NRMSE Δ = −0.006" (SNI slightly worse) points the **other way** in median terms — SNI is better in 8 of 12 settings (Cliff δ = +0.33) | `posthoc_wilcoxon_holm_effectsize_ci.csv` | **fixed-P1** (both statistics reported) | ESM Table S3; main `:475` |
| **B56** ⚠️ NEW | `MICE_v3.py:329` raises `IndexingError` when handed a non-contiguous index, which blocks any within-fold protocol that slices folds. Found while wiring `evaluation/protocol.py` | `baselines/MICE_v3.py:329` | **worked around in P1** (fold blocks are passed with a reset index, which is also required so an imputer cannot condition on a row's position in the original table); the underlying fragility is **pending-P2** | — |

### From T1.4, the baseline port

| ID | Problem | Evidence | Disposition | Affected |
|---|---|---|---|---|
| **B66** ⚠️ NEW | **MICE has the same seeding defect as SNI (B48).** `MICE_v3.py:283` derives each round's seed as `self.seed + it*1000 + hash(col) % 1000`, and CPython salts `str.__hash__` with `PYTHONHASHSEED`, which is randomised per process. The manifest's `seed` column therefore does not determine a MICE run. Measured on MIMIC with the manifest seed held fixed: NRMSE 0.09046–0.09604 across five `PYTHONHASHSEED` values, a spread of about **6 %** — more than three times the entire de-leaking effect (1.7 %) | `MICE_v3.py:283`; `results/T1.4_deleak/hashseed_sensitivity.csv` | **contained in P1** — every T1.4 MICE run pinned `PYTHONHASHSEED=0` and recorded it in `run_config.json`; the underlying fix is the same decision as B48 (Q1-8) | ESM §S4 reproducibility notes; any MICE number in the paper |
| **B67** ⚠️ NEW | The `hyperimpute` package's `fit`/`transform` is a facade: `plugin_hyperimpute.py:124-125` defines `_fit` as `return self` and `:127-128` defines `_transform` as `self.model.fit_transform(X)`, so the training fold has no influence whatsoever. Verified empirically — `p.fit(train); p.transform(test)` is elementwise identical to a fresh plugin's `fit_transform(test)`. This is a defect of the published package, not of our harness | `hyperimpute` 0.1.17 `plugins/imputers/plugin_hyperimpute.py`; test `test_hyperimpute_package_fit_transform_is_a_facade` | **documented-P1** — HyperImpute is classified non-separable and routed to the fold-independent protocol. **decision-needed** (Q1-22): whether to name the package defect in the manuscript | R1-3 response |
| **B68** ⚠️ NEW | In the candidate MIMIC replacement table, `vasopressor_use` is a deterministic function of `vasopressor_dose`: `vasopressor_use == (vasopressor_dose > 0)` holds for **2939/2939 rows**. This is the same class of defect as eICU's `age_band` (B41) and the seven derived columns of F10 — a column that cannot carry information the imputer does not already have, but that inflates the feature count and gets a free perfect score on any metric averaged over columns | `~/data_MIMIC_ICU/mimic_icu_mortality_real.csv`; runtime precondition check in `data_layer/build_mimic.py` (raises if it ever fails) | **pending-P2** — dropped by `build_mimic.py`, extending F1–F10 to F11. Only relevant if the replacement is adopted (Q1-6) | — |
| **B69** ⚠️ NEW | `vasopressor_dose` in the same table **mixes two units in one column**. On a log scale the non-zero doses are bimodal: a main mode over 10⁻¹–10¹ (norepinephrine equivalent, mcg/kg/min, physiologically sensible), a near-empty gap at 10^1.5–10² holding only 4 values, then a second mode of **197 values (15.3 % of non-zero doses)** at 10²–10³. A dose of several hundred mcg/kg/min is not survivable; those rows are almost certainly mcg/min or ng/kg/min. This refines F5, which recorded only that the maximum was implausible | histogram of `log10(vasopressor_dose)` over non-zero rows, n=1288 | **pending-P2** — winsorized at 10.0 by `build_mimic.py` and **disclosed**, not silently smoothed. Which row uses which unit is unrecoverable without database access, so the column is a declared limitation rather than a repaired one | — |
| **B70** ⚠️ NEW | **TabCSDI diverges on half the benchmark, and the published table averages the divergence into a number.** Across R0's 60 recorded TabCSDI runs the continuous R² is healthy on AutoMPG, ComCri and MIMIC (0/10 negative each) but collapses on the other three: eICU 9/10 negative, Concrete 8/10 negative with 7 below −1 (min −8.57), NHANES 8/10 negative with 7 below −1 (min **−223.99**, mean **−28.90**). On NHANES MAR@30 % the five seeds give R² = +0.007, +0.161, −19.35, −224.0, −8.70 — a spread across which an arithmetic mean has no meaning. The collapsed runs also terminate early (133–144 s against 196–214 s for the healthy ones) with mean bias up to +103.7, so the model is diverging rather than merely fitting poorly. Reproduced **bit-for-bit** by the T2.0(b) gate, all 10 runs × 9 metrics, max difference exactly 0.0 — so this is R0's own behavior faithfully carried over, not a porting artifact | `results_all/agg_baselines_new/summary_all.csv` (R0's own record); `results/T2.0_gate/tabcsdi/summary_gpu_legacy.csv`; verdict in `results/T2.0_gate/verdict.json` | **decision-needed** — a diverged model is not a competitor, and reporting its mean as a baseline overstates SNI's margin on exactly the datasets where the margin is largest. Options: report median instead of mean, report the divergence rate alongside, or exclude TabCSDI from the affected datasets with the reason stated. Whichever is chosen must be disclosed | Table 1 TabCSDI row; any TabCSDI comparison on eICU/Concrete/NHANES; R2-6 |
| **B71** ⚠️ NEW | **R0's NHANES table contains blood pressures of zero.** `fm_NHANES_toCSV_forSNI_v1.py:138-140` averages `BPXDI1/2/3` with no handling of NHANES's failed-measurement code, so the shipped `NHANES_complete.csv` has **8 rows with a diastolic pressure below 1 mmHg and 11 below 20**, minimum 0.0. Two compounding traps: (i) `pandas.read_sas` decodes the SAS zero out of XPT's IBM hexadecimal float as the denormal **5.397605346934028e-79**, not as 0.0, so an `== 0` guard matches nothing — BPXDI1/2/3 contain *zero* exact zeros against 134/124/127 values in (0,1); (ii) averaging happens before any filtering, so one failed inflation drags the mean down and manufactures values like 2.7 mmHg. Of 19,018 otherwise-valid diastolic readings only 13 (0.068 %) fall below 20 and the 0.1st percentile is 24, so a 20 mmHg floor is read off the data rather than asserted; the systolic side is untouched (minimum valid reading 72) | `fm_NHANES_toCSV_forSNI_v1.py:138-140`; measured on `BPX_J.XPT`; `project_sni_R0/sni/data/NHANES_complete.csv` | **fixed-P2** — `data_layer/build_nhanes.py` drops readings below 20 mmHg *before* averaging and records the count per column. Costs 10 rows (2252 → 2242). Note the first version of our own fix reproduced trap (i) and silently converted nothing | any NHANES number; NRMSE normalization in particular, since the corrupted values sit at the extreme of the column |
| **B72** ⚠️ NEW | **The complete-case construction destroys the one missingness mechanism NHANES can actually verify.** NHANES runs the fasting-dependent assays only on the morning fasting subsample — in the 2017-2018 cycle triglycerides are missing for 95.0 % of non-fasting participants against 20.6 % of fasting ones — so it is the benchmark's only mechanism that can be checked against the source rather than argued for. But dropping rows with any missing value removes almost every non-fasting participant along with their missing labs: the rebuilt table is **2150 fasting against 92 non-fasting (95.9 %)**, against **55.2 %** among the 5410 adults with a recorded fasting status before filtering — 96.2 % of the non-fasting participants (2422 → 92) are discarded. (An earlier draft of this entry quoted 46.8 %, which is the prevalence over the full 9254-row merge including minors; the adult figure is the one that matches the benchmark's own cohort.) `fasting_state_std` is therefore near-constant on the table it would have to drive, and any MAR mechanism built on it is unreachable by construction. The same argument applies to every "complete-case then re-mask" benchmark: the rows discarded to make the table complete are exactly the rows in which the real mechanism is visible | `data/derived/nhanes_build_report.json` (prevalence 0.468 before complete-case); `data/derived/NHANES_complete.csv` (0.959 after); measured contrast on the masked columns is undefined, not small, because the two deciles of a 95.9 % constant are the same rows | **decision-needed** — three options: (i) keep `fasting_state_std` as a declared-weak driver and state the limitation, (ii) drop it and drive NHANES MAR from `age`/`smoking_std` alone, (iii) reconstruct NHANES without complete-casing the fasting labs. The `_calibration` block in `configs/missingness.yaml` currently names a target contrast of 4.29/4.36 that this table cannot reach, and must be corrected either way | ESM data construction; the R1-4 response, which currently cites NHANES as the strongest MAR example |
| **B73** ⚠️ NEW | **GAIN never beats the column mean, on any dataset, and the table does not show it.** Across R0's own 300 GAIN runs the continuous R² median is negative on all six datasets — AutoMPG −0.842, ComCri −0.266, Concrete −0.623, MIMIC −1.180, NHANES −1.680, eICU −4.147 — so a constant-mean imputer outperforms it everywhere. On NHANES MAR@30 % specifically the range is [−8.889, −1.134]. Our T2b.3 smoke test reproduced −2.635 there, inside R0's range, confirming this is the method's behavior and not a harness artifact. Two consequences: (i) presenting GAIN as a competitive baseline overstates the field SNI is compared against, in the same direction as B70's TabCSDI divergence; (ii) any mean-based aggregate that includes GAIN is dragged by a method that is worse than the trivial imputer | pooled `results_all/agg_baselines_{main,deep,mnar}/summary_all.csv`, n=300; `results/T2b_smoke/` | **covered by P2b decision 1** — the divergence-rate column now required for *every* method will surface this without special-casing, and the median aggregate is robust to it. Whether to keep GAIN in the main table or move it to the ESM is **decision-needed** | Table 1 GAIN row; R2-6 (fairness of baseline treatment) |

| **B74** ⚠️ NEW | **A run record can claim the wrong `PYTHONHASHSEED`.** `common/determinism.py:65` sets `os.environ["PYTHONHASHSEED"] = str(seed)`, a faithful port of R0's `utils.py:15-31`. Per B48 that assignment has no effect on the running interpreter — but it does overwrite the variable, so any code reading it afterwards sees the per-run seed instead of the value the interpreter started with. A run launched with `PYTHONHASHSEED=2025` and seeded `seed=1` recorded `"pythonhashseed": "1"` in its `run_config.json`; anyone reproducing from that record would export 1, get different per-feature seeds, and fail to match. Caught in our own pipeline, not R0's, while spot-checking the first `run_config.json` the grid runner wrote | `common/determinism.py:65`; measured: startup 2025, `os.environ` after `apply(seed=7)` = 7 | **fixed-P2b** — `determinism.STARTUP_PYTHONHASHSEED` captures the value at import, before anything can overwrite it, and that is what every `run_config.json` records. The R0-faithful assignment is deliberately left in place so the equivalence gate still holds | ESM §S4 reproducibility notes; any claim that a run is reproducible from its recorded config |
| **B75** ⚠️ NEW | **Regenerating a mask mid-experiment is undetectable by the row-space guard.** P2b decision 3 added a row-space fingerprint so a shuffled mask cannot be paired with an unshuffled table. It guards row *order*. It cannot see a mask whose **content** changed while the row order stayed the same — and that is what happened here: decision 2(b) regenerated NHANES's masks (same shuffled table, same row order, `fasting_state_std` dropped as a driver) 7 minutes after the TabCSDI budget experiment started, so its 1x arm ran on the old mask and its 2x/4x arms on the new one. Nothing raised. The contamination surfaced only because a later probe disagreed with the earlier run at identical settings (R² −1.763 vs −4.217). Our own process error, not R0's | `results/T2b_tabcsdi_budget/budget_runs_VOID_mixed_masks.csv`; mask mtime 14:23:17 against experiment start 14:16 | **fixed-P2b** — every grid run records the mask file's md5 in `metrics_summary.json` and `run_config.json`, and `stats/aggregate_grid.mask_consistency` asserts that all seeds of a cell used one mask, printing any cell that did not. Two regression tests reconstruct the failure. The affected NHANES arm was voided and re-run | any experiment spanning a config change; the ESM would otherwise have carried a cross-mask comparison as evidence |
| **B76** ⚠️ NEW | **`epochs` controls two things at once, so raising it trains the model *less*.** In both `TabCSDI_v1.py` (`:417`, `:436-495`) and `sni/imputer.py` (`:129-132`, `:827`, `:946`), `epochs` is simultaneously the training cap **and** `T_max` of a `CosineAnnealingLR`, alongside early stopping on the **training** loss with patience 20 and `min_epochs` 50. Raising the cap slows the LR decay, which makes the loss noisier, which makes 20 consecutive non-improvements *more* likely — so training stops sooner at a higher learning rate. Measured on NHANES MAR@30 %, TabCSDI, seed 1: cap 200 → **136 epochs**, R² −1.76; cap 800 → **52 epochs**, R² −12.94. Four times the budget produced 38 % of the training. Forcing the full budget instead settles it completely. NHANES MAR@30 %, seed 1, in order of **actual** training: 52 epochs → R² −12.94, 136 → −1.76, 201 → −0.76, **801 → +0.370**. Monotone in epochs actually run, and the endpoint is in the same class as MissForest (0.487) and MIWAE (0.508) in R0's own record. The reported "divergence" is not a property of the method at all — it is **severe under-training**, and the paper currently compares SNI against a baseline trained for 52-136 epochs where 800 produces a competent one | `results/T2b_tabcsdi_budget/epoch_probe.csv`; the non-monotone budget result it explains is in `budget_runs_VOID_mixed_masks.csv` | **decision-needed** — the same configuration is in SNI, so relaxing it for the competitor alone would be exactly the asymmetry R2-6 asks about, while relaxing it for both breaks fidelity to R0 and the premise of gate (a). A symmetry probe on SNI is running. Options and costs are in (private development records) | Table 1 TabCSDI row and B70's framing; R2-6 (fairness of baseline treatment); ESM training-protocol description |
| **B77** ⚠️ NEW | **ESM misstates TabCSDI's training budget.** `ESM_1_SNI_HISC_v5_5.tex:234` says TabCSDI has "manifest-specified training budgets (default **100 epochs** and 50 diffusion steps)". The dataclass default in `project_sni_R0/sni/baselines/registry.py:300-309` is **`epochs=200`**, `diffusion_steps=50`, and R0's `budget_params` is empty for this method, so 200 is what ran. The diffusion-step figure is right; the epoch figure is wrong by a factor of two. Same class as B1 (declared value ≠ implemented value) | `ESM:234` vs `registry.py:307`; `manifest_baselines_new.csv` `budget_params` empty for TabCSDI | **fixed-P2c (documentation)** — the ESM figure is corrected to 200. Combined with B76 this makes the R1 account self-consistent: the configured budget was 200; the ESM recorded 100; the *actual* training was truncated to 52-136 epochs by a defective stopping rule; R1 executes the configured 200 | ESM §S3.4 `:234`; any statement about baseline training budgets; R2-6 (fairness of baseline treatment) |
| **B78** ⚠️ NEW | **Past ~400, the epoch budget is an inert knob under the default stopping rule.** A corollary of B76, and stronger than it: raising the cap does not merely stop training *sooner*, it eventually stops it at the **identical** epoch, because once `T_max` is large the cosine LR is effectively flat across the whole range in which patience can fire. Fitting `runtime = 85.44 s + 1.0353 s/epoch` to the two seed-1 anchors from the epoch probe recovers the other probe points to within one epoch (seed 2 cap 200: predicted 124.8 vs measured **125**; seed 2 cap 800: predicted 83.9 vs measured **84**), and applied to the budget runs it gives cap 400 and cap 800 the same stopping epoch on every seed — **52.0/52.0, 83.9/83.9, 85.9/85.9**. Hence the two arms' wall-clocks agree to 0.18 ms, 25 ms and 8.2 ms, and both are *faster* than the 200 arm. So a "budget sensitivity" study run under the default stopping rule cannot measure anything above 400: the arms collapse onto one run | `results/T2b_tabcsdi_budget/budget_runs.csv` runtimes vs `epoch_probe_TabCSDI.csv` `epochs_run`; model fitted on seed 1 alone and validated on two held-out probe points | **no code change needed** — `tests/budget_sensitivity.py:63-67` already passes `disable_early_stop=True` for exactly this reason, so T2c.3's curves measure training actually performed. Recorded because it is the quantitative form of B76 and because any future budget study that forgets the flag will silently produce duplicate arms | **ESM material (P2d §5 directs registration)** — any budget-sensitivity study that keeps the default stopping rule has its arms above ~400 collapse onto a single run, so a published "we varied the budget" claim can be vacuous without anyone noticing; T2c.3 budget-sensitivity curves; B76's framing |
| **B79** ⚠️ NEW | **Parallel smoke workers overwrote one another's results file; 42 of 49 rows were lost silently.** The CPU smoke arm was run as three workers split by `--datasets`, but `tests/smoke_nine_methods.py` derived its output name from the *queue* alone, so all three wrote `results/T2b_smoke/smoke_cpu.csv`. The last to finish won: the surviving file holds 7 rows (CDC2022 only) where 49 runs completed. Nothing raised — the file exists, parses, and looks like a result. Same class as the epoch-probe overwrite (`epoch_probe.csv`), and the second time this pattern has cost data. Our own process error, not R0's | `smoke_cpu.csv` 7 rows vs `rescored.csv` 49; `cpu_w1/w2/w3.log` cover CDC2022 / eICU+NHANES / AutoMPG+ComCri+Concrete+MIMIC respectively | **fixed-P2c** — the suffix now includes the dataset selection whenever it is a strict subset of the configured tables, so concurrent workers are collision-proof without the launcher passing anything, and the row count is printed on write. No result was lost in the end: `tests/rescore_smoke.py` rebuilds the table from the per-worker logs, which is why the criterion-6 verdict still rests on all 49 runs. **The full grid is not exposed to this** — `experiments/run_grid.py` writes per-cell artifact directories and resumes by artifact existence, so shards cannot collide | smoke-test evidence for the MIMIC 8/06 fallback gate; any future re-run of the smoke arm |
| **B80** ⚠️ NEW | **A probe that measured nothing reported success.** `tests/concurrency_probe.py` launched its children with `stderr=subprocess.DEVNULL`, counted a run only if it printed `ELAPSED`, and returned 0 regardless. Every child of the first T2c.1 sweep died in ~0.3 s on `MKL_THREADING_LAYER=INTEL is incompatible with libgomp.so.1` — the generated child imported `torch` before `numpy`, so libgomp was loaded before mkl-service initialized. The sweep printed `completed=0/N` for all 22 settings, wrote a well-formed CSV, exited 0, and the chain advanced to the next stage as though the queue had been measured. **The whole of T2c.1 took twelve seconds and produced nothing.** Exactly R0's `baselines_deep` failure mode — 300 runs that died silently and were reported as a result — reproduced in our own harness | `results/T2c_concurrency/gpu_sweep.csv` (22 rows, `n_completed` 0 in all); `chain8.progress` shows stages 1/3 and 2/3 at rc=0, 5 s and 7 s apart | **fixed-P2c** — children now import numpy first; stderr is captured and the first two failures are printed per setting; and the sweep returns 1 when nothing completed, with a warning when only some did. Our own process error, not R0's | **ESM corrections discussion (P2d §0 directs this explicitly)** — this is the *second independent* instance of the same failure mode, the first being R0's 300 silent `baselines_deep` deaths. Two independent occurrences in two separate codebases is evidence that silent failure is a systemic risk of this kind of harness rather than an individual oversight, and the ESM should say so rather than presenting R0's case as a one-off; T2c.1 concurrency conclusions; §4 of the P2c brief |
| **B81** ⚠️ NEW | **Two concurrent GPU jobs cost 172x, and low GPU utilisation is the cause rather than the exemption.** Measured by accident and therefore over a long window: an orphaned `budget_sensitivity.py` (killed shell, surviving child, PPID 1) shared the card with the smoke arm for 6 h 21 m. `SNI/AutoMPG` — a 392x7 table, modeled at ~128 s — took **21 969.2 s**. The orphan was killed at 10:15; `TabCSDI/AutoMPG` completed **36.9 s** later. Load average throughout was 2.06 on 32 cores, so this is **not** host CPU over-subscription, which is what P1's 80x note and our own earlier reading both assumed. The mechanism is the opposite of the one we inferred: SNI is millions of microsecond-long kernels (60/60 samples at 0 % utilisation, 584 MiB), and without MPS two CUDA contexts time-slice, so each switch costs far more than the kernel it serves. **Low utilisation is why concurrency is catastrophic, not why it is safe.** P1's 80x was real and if anything understated | `results/T2b_smoke/gpu.log` `SNI/AutoMPG … 21969.2s` against `TabCSDI/AutoMPG … 36.9s` post-kill; `ps` PPID 1 on pid 1103780; `uptime` load 2.06 | **annotated** — `configs/scheduling.yaml`'s existing "one GPU job at a time" policy is **correct and stays**. Supersedes §4.1 of the P2c brief, which argued the queue could widen to 2-3; that argument is withdrawn. Consequence for scheduling: GPU wall-clock equals GPU machine-hours, so P2c §7 condition 2 is triggered under **both** readings of "100 机时", not just the machine-hours one | P2c brief §4 and §6; P2c §7 condition 2; any claim that the grid can be shortened by widening the GPU queue |
| **B82** ⚠️ NEW | **A CPU-queue method silently opens a CUDA context and costs the GPU queue 2.92x.** `HyperImpute` is placed on the CPU queue by `configs/scheduling.yaml`, and nothing in its code says otherwise (`registry.py:26-32` discusses fit/transform separation, not devices) — but it allocates ~440 MiB of GPU memory per process: 1326 / 2649 / 5297 / 7060 MiB at 3 / 6 / 12 / 16 workers in the T2c.1 sweep, against a flat 3 MiB for KNN in the same batches. Measured directly (T2d.2): one TabCSDI cell takes **328.9 s** alone, **958.9 s** beside 12 HyperImpute workers (**2.92x**), and **342.5 s** beside the same 12 with CUDA hidden from them (1.04x). The schedule assumes the two queues cost each other about 1 %, and the only evidence for that was gate T2.0(c) — **one** CPU job beside **one** GPU job, n=10. Left in place this turns a 238 h GPU queue into roughly 700 h, and it would have looked like the grid simply being slow | `results/T2d_device/hyperimpute_interference.csv`; GPU memory column of `results/T2c_concurrency/cpu_sweep.csv` | **fixed-P2d** — `experiments/run_grid.py` sets `CUDA_VISIBLE_DEVICES=""` when `--queue cpu`, which is the cleanest of the three remedies P2d lists: no queue is re-assigned and no concurrency is throttled. Setting an env var inside a running interpreter is the shape of B48, so the effect is **verified rather than assumed** — the run refuses to start if `torch.cuda.device_count()` is not 0 afterwards. All seven CPU-queue methods were re-checked under the change, including the two neural ones (MIWAE R²=0.7841, HyperImpute R²=0.6901 on AutoMPG) | grid wall-clock and every schedule figure that assumed 1 % co-residency; ESM Table S6 device statement (with B2) |
| **B83** ⚠️ NEW | **SNI's results are device-dependent by construction: the same seed on CPU and CUDA trains different models.** `sni/cpfa.py` applies dropout in 11 places, including `dropout=0.1` on all four `nn.MultiheadAttention` modules (`:35`, `:83`, `:84`, `:177`). Dropout draws from the generator of the device the tensor lives on, and torch's CPU generator (Mersenne Twister) and CUDA generator (Philox) produce **different sequences from the same seed** — measured: seed 1 gives CPU `[0.7576, 0.2793, 0.4031, 0.7347]` and CUDA `[0.8903, 0.0275, 0.9031, 0.5386]`. So the dropout patterns differ, the training trajectories differ, and the fitted models differ systematically. Everything else is device-independent, which is what isolates this: the pseudo-mask is `np.random.rand` (`imputer.py:792`), the model is initialized on CPU then moved (`:824`), and TF32 is off in deterministic mode. **Magnitude on MIMIC MAR@30 %:** three CUDA seeds give R² 0.0889 / 0.0838 / 0.0797 (spread 0.0092) and three CPU seeds give −0.0383 / −0.0441 / −0.0402 (spread 0.0058) — two tight, **disjoint** clusters 0.118 apart, i.e. **the sign of R² flips with the device**. Reproducible on both sides (CPU seed 1 gave −0.0383 twice; CUDA seed 1 gave 0.0889 in two independent runs). Not a defect in SNI — inherent to PyTorch — but its consequences are not documentation-level | `results/T2d_device/C1_single_cpu.csv`, `C3_mimic_cpu_seeds.csv`, `protocol_pairs.csv`; RNG divergence measured directly | **annotated + escalates B2** — SNI stays on the GPU queue (T2d.1 verdict FAILS on C3 for exactly this reason), so no numbers change. But this turns **B2 from a documentation error into a reproducibility one**: the ESM says every experiment ran on CPU while SNI ran on an A6000, and on MIMIC the CPU cannot reproduce the published sign, let alone the value. The device must be stated per method, and the ESM must say that SNI's figures are device-specific | ESM Table S6 `:530` and the B2 correction; ESM §S4 reproducibility notes; any statement that a fixed seed reproduces SNI |
| **B84** ⚠️ NEW | **An SNI run on CPU may not be determined by its seed: the BLAS thread count appears to change the result.** Observed by accident during T2.0: a pilot run and the gate's own run of the **same** dataset, seed, codebase and startup `PYTHONHASHSEED` (`hash_probe` 3944 in both) gave `cat_Accuracy` 0.307482587724 vs 0.310849145115 (3.4e-3) and `cont_MB` 0.318261400242 vs 0.293648230801 (2.5e-2). The only known difference was the thread count. Mechanism, if confirmed: floating-point non-associativity in parallel reductions — the same *class* of problem as B48 (a recorded seed failing to determine a run) but arising from parallelism rather than hash randomization. `tests/thread_sensitivity.py` was written to settle it and **was never run**; nothing about thread sensitivity was in this ledger until now | the two `metrics_summary.json` files quoted in `tests/thread_sensitivity.py:1-20`; sweep queued (`chain15`, threads 1/8/24/32, MIMIC seed 1, port side) | **CONFIRMED 2026-07-30.** The sweep ran: MIMIC seed 1, port side, threads 1/8/24/32, identical `seed`, `hash_probe` (3944) and `convergence_iterations` (3) in all four. Every metric moves. `cat_Macro-F1` spread **5.20e-2**, `cat_Accuracy` **3.75e-2**, `cat_Cohen_kappa` 3.83e-2, `cont_R²` 3.34e-3, `lambda_mean` 8.08e-3. For scale, R0's Table S3 headline is SNI beating GAIN by Macro-F1 **+0.236** — so thread count alone moves that metric by **22 % of the effect the paper reports**. Mechanism: floating-point non-associativity in parallel reductions. `results/T2.0_gate/thread_sensitivity/thread_sensitivity_port_MIMIC_s1.csv`. **This defeats the first stated reason in P2e §3.1 for making CPU canonical** — a reader on different hardware does not reproduce our categorical numbers. The ESM must pin the thread count as part of the reproduction recipe, and `SNI_EQ_THREADS`-style pinning must extend to the grid runner, not just the gates. It is registered now because P2e §3.1 makes CPU the canonical device for SNI and its **first** stated reason is that a reader following the ESM can reproduce our numbers on ordinary hardware. If the result depends on core count, that reason fails and the ESM must pin the thread count as part of the reproduction recipe. **The equivalence gates are unaffected**: both sides run at one fixed thread count (`SNI_EQ_THREADS`, default 32) and agree bit for bit there | ESM §S4 reproducibility notes, jointly with B48 and B83; the P2e §3.1 justification for moving SNI to CPU |
> **Not a finding — a records defect (B74 recurring).** The T2.0 gate artifacts record `pythonhashseed: '5734'`/`'5735'` while `hash_probe` is 3944 in every run — and 3944 is the value for **2025**, not 5734 (measured: 5734 gives 9023, 5735 gives 8787). The field is read with `os.environ.get("PYTHONHASHSEED")` at the *end* of a run (`gate_equivalence.py:162`), by which point R0's `set_global_seed` has overwritten the variable with `cfg.seed + hash(f) % 10000` for the last feature trained. So the artifacts misreport their own provenance; `hash_probe` is the trustworthy witness, is identical across all runs, and confirms every gate run used 2025. **The gate verdict stands** — both sides of every pair ran under identical conditions and agreed bit for bit. Only the recorded label is wrong, in artifacts written before B74 was found.
> **Not a finding — a correction.** T1.4 reported that the GPU had entered a fault state (`nvidia-smi` `ERR!`, a minimal CUDA benchmark not returning within 120 s) and ran TabCSDI on CPU as a result. Re-checked afterwards: allocation plus `synchronize()` takes 0.31 s and a matmul 0.08 s — the device is healthy. The `ERR!` is on the **fan-speed** field only and was already present in the P0 scan at 12:09, before any R1 job started; temperature and power draw are normal. The 120 s timeout was single-GPU queue saturation from ten concurrent equivalence workers plus T1.4's own joblib pool — the same phenomenon that killed all 300 `baselines_deep` runs in R0. No operations intervention is needed; the real constraint is that **effective parallelism on one GPU is about 4**.

### From T1.6, the data-layer analysis

These were numbered B48–B56 inside `results/T1.6_datalayer/T1.6_report.md`; that numbering collided with the IDs already issued above, so they are **renumbered B57–B65 here**. The T1.6 report's own §6.7 is the detailed version.

| ID | T1.6 ID | Problem | Evidence | Disposition | Affected |
|---|---|---|---|---|---|
| **B57** | its B48 | The two eICU zero-variance columns act in **opposite** directions, so P0's description of both as "diluting RMSE" is imprecise: `hours_since_admission` dilutes `cont_NRMSE` (0.09 % of it), whereas `vasopressor_use_std` hands `cat_Accuracy` and `cat_Macro-F1` a perfect score at 1/5 weight — exactly 1.0000 in 90/90 runs | `percolumn_meanerror_eICU.csv`, `percolumn_accuracy_by_method_eICU.csv` | **decision-needed** with B35 | main Table 1; ESM Table S5 |
| **B58** | its B49 | Table 3's protocol note attributes eICU's lower D stability to dimensionality, but **at least 11 % of it comes from the uniform D rows of the zero-variance columns** (ρ rises 0.633 → 0.703 once they are dropped; `vasopressor_use_std`'s D row has CV = 0.00029, max/min = 1.00116). Separately, ESM Fig. S7's "approximately linear in d" rests on 6 points with r = 0.703, R² = 0.494 over d ∈ [8, 20]; changing eICU's d moves the slope by +21 % | `SNI_HISC_v5_5.tex:365`; `ESM:536-537`; `B35_table3_D_stability_variants.csv` | **pending-P3** (Table 3 will be recomputed there anyway) | main Table 3; ESM Fig. S7 |
| **B59** | its B50 | **`eICU_complete.csv`'s `urine_output_min` carries a unit or aggregation error**: range 0–220,912, median 4117.5, p90 33,649, and 43.78 % of values exceed any physiological bound. It alone accounts for **98.54 % of eICU's reported `cont_MAE`**. The same variable in `~/data_MIMIC_ICU/mimic_icu_mortality_real.csv` runs 1.0–1400 mL/h with median 30.0, and both tables come from the same "ICU LGU" column template | `column_profile_health.csv`, `column_share_of_summary_metric.csv`, `mimic_vs_eicu_schema_comparison.csv` | **decision-needed** (Q1-19) — supersedes the milder B47 framing | main Table 1 MAE column; ESM Table S5 |
| **B60** | its B51 | The NHANES `_NoMis` complete-case step has **no producer script in either tree** — the NHANES counterpart of B36. It was verified bit-identical to `dropna()` over 11 X columns, but the step itself is undocumented | repo-wide grep for `NoMis` returns nothing | **pending-P5** (document in provenance) | reproducibility statement |
| **B61** | its B52 | `fm_NHANES_toCSV_forSNI_v1.py` **degrades silently** when the 4 XPT modules are absent: it emits 11 features instead of 16, without an error or a default | `:59, 77-78, 176, 181, 186, 192, 198, 289-293` | **pending-P2** if the modules are downloaded | data build |
| **B62** | its B53 | **The health-dataset numbers and the two gap percentages in `SNI_HISC_v5_5.tex:262` use MAR @ 30 % only**, and the text does not say so. Recomputing MAR-only gives +14.80 % / −18.96 % (matching the paper); MCAR+MAR gives +16.58 % / −19.17 % | `paper_gap_sentence_by_scenario_MAR.csv` | **pending-P5** (state the conditioning) | main `:262` |
| **B63** | its B54 | `fm_NHANES_toCSV_forSNI_v1.py:202` dropped the `.fillna(0)` that v4.3 had at `:190`, so a missing `PHAFSTMN` silently marks a fasting participant as non-fasting. Zero rows diverge in the J cycle, but it is a live hazard if the script is reused on another cycle | `:202` vs `fm_NHANES_toCSV_v4_3.py:190` | **pending-P2** if NHANES is rebuilt | data build |
| **B64** | its B55 | The candidate MIMIC replacement and its extract script carry **10 defects (F1–F10)**, three of them blocking: the `GCS` itemid is mislabelled (220739 is Eye Opening, not the total, so the column holds only {3,4}), the shipped CSV is inconsistent with the current script (`PF_ratio`), and the script contains a plaintext database password plus an unseeded `ORDER BY RANDOM()` | `mimic_extract_v7.py:265-268, 280-281, 300, 511-518, 601, 676, 771, 847, 87-88` | **decision-needed** — only relevant if the replacement is adopted (Q1-6) | — |
| **B65** | its B56 | `fm_MIMIC_toCSV_v2d.py:210-223` has a key-name mismatch that leaves `output/ground_truth.json` covering only **5 of 20 variables (25 %)** — `gt_all` uses `"map"/"lactate"/"creatinine"/"age"` while the columns are `map_mmhg`/`lactate_mmol_l`/`creatinine_mg_dl`/`age_years`, so the MAP=65, lactate=2/4 and creatinine=1.2/2.0 thresholds are all lost. `sanity_report.json` records `ground_truth_coverage_ratio: 0.25` and 15 gaps | `fm_MIMIC_toCSV_v2d.py:210-223`; `~/data_MIMIC_ICU/sanity_report.json` | **decision-needed** — only relevant if the replacement is adopted | — |
| **B20** | `06_gen_supp_tables.py:355-360` caption states the opposite sign convention to the actual computation; the manuscript was hand-corrected but the generator was not | generator vs `exp5:566-570`; verified numerically | **fixed-P1** (new generator, E5) | ESM `:353` (already correct — do not regress) |
| **B13** | Measured D-vs-SHAP Spearman is non-significant and negative for ALARM (−0.357, p=0.432), single seed, n=7 sources, while the text claims the rankings overlap | `ext2/table_S7_shap_vs_D/MIMIC/spearman_d_vs_shap.csv` | **pending-P3** | main `:336`; Fig. 4b |
| **B12** | Two incompatible cross-seed ρ definitions coexist: Fig. 4b reports 0.914–1.000 (per-target rows, 2 targets, string hardcoded) and Table 3 reports 0.951 (full matrix, off-diagonal) | `10_paper_selection.py:261-266` vs `11_revision_artifacts.py:334-361` | **pending-P3** (unify) | main Fig. 4b `:345`; Table 3 `:348-369` |
| **B18** | Table 2 and the proxy experiment used heavily reduced hyperparameters (epochs 50/80, heads 4/8, emb 32/64, max_iters 2) versus the main configuration (200/16/128/3) | `run_all_experiments.py:236-251`; `exp1_audit_story_leakage.py:308-316` | **pending-P3** | main Table 2 `:307-331`, Table 4 |
| **B19** | Hub-ρ uses a hand-rolled rank correlation that does not handle ties; the XOR regime has out-degrees in {0,1,2}, so ties are pervasive — a likely cause of the −0.281/−0.208 values | `sanity_check_v2_s5.py:159-179`, esp. `:168-172` | **pending-P3** (one-line switch to `scipy.stats.spearmanr`) | main Table 2 Hub ρ column |

---

## Tier 3 — reproducibility and engineering

| ID | Problem | Disposition |
|---|---|---|
| **B10** | GPU path disabled determinism (`enable_performance_mode` reversed the cudnn flags and enabled TF32); `enable_torch_determinism()` was never called | **fixed-P1** — `common/determinism.py`, determinism on by default, mode recorded in `run_config.json` (E3) |
| **B22** | SNI runs wrote no `run_config.json`; hyperparameters were not recoverable from a run directory | **fixed-P1** — `common/runconfig.py`, every run writes one (E2) |
| **B38** | `mask.npy` was never loaded by any code path | **fixed-P1** — `common/masks.py` loads the `.npy` and asserts agreement with `X_missing.isna()` (E4) |
| **B24** | `datasets.yaml` drove nothing (sole consumer `12_revision_B_tasks.py:66`); `recommended_cat_balance_mode` was flattened to `none` everywhere; the manifest generator was not in the repo | **fixed-P1** — configs drive the runners, manifests are generated (E1) |
| **B23** | `docs/evidence_map.md`, `docs/results_structure.md` and `data/manifest_README.md` were all stale (wrong output paths, wrong naming scheme, wrong manifest row counts) | **fixed-P1** — new `docs/evidence_map.md` (T1.8), old ones not carried over |
| **B14** | The Zenodo archive very likely lacks the paper-cited files; not verifiable locally | **resolved by decision** — R1 ships GitHub only; the Zenodo citation in the manuscript needs handling (**pending-P5**) |
| **B15** | Working tree dirty, no tag at HEAD | **fixed-P1** — commit `10df537`, tag `v0.5-R0-submitted` |
| **B21** | `merged/merged_summary_agg.csv` contains the 300 failed deep runs (runtime ≈ 0.007 s), producing 65 conflicts | **fixed-P1** — `code_SNI/stats` excludes `agg_baselines_deep` explicitly and asserts the exclusion |
| **B16** | Disk was 96 % full | **resolved** — first author freed space; 506 GB now available |
| **B26** | `11_revision_artifacts.py:210` hardcodes `"XGBoost-based pipeline"` for what is a logistic-regression pipeline; the wrong string reached `outputs_paper/csv/impute_predict_expanded.csv` | **fixed-P1** — the new reporting layer derives the model name from the run config |
| **B25** | `split_manifest_runner.py:81` referenced a non-existent `scripts/run_manifest.py` | **wont-fix** — script not carried over into `code_SNI` |
| **B27** | LaTeX warnings: "Text page 10 contains only floats" ×4; `Overfull \hbox` 12.41 pt at `:216`; 8 bibliography entries missing publisher/pages | **pending-P5** |
| **B28** | Main text references the ESM by hand-written strings rather than `\ref`, so supplementary numbering must be synchronised manually | **pending-P5** |
| **B29** | Default `python` is the base env without torch; `statsmodels` was missing | **fixed-P1** — `statsmodels==0.14.6` + `patsy==1.0.2` installed and pinned; `requirements.lock.txt` records the change |
| **B30** | `nvidia-smi` reports `ERR!` for fan speed | **annotated** — cosmetic; temperature and power draw are normal |
| **B31** | A second checkout at (a private local path, recorded in the private development history) was the real R0 run location | **fixed-P1** — archived to (a private local path, recorded in the private development history) with `ARCHIVED_README.txt` |
| **B40** | `reviewer_risk_checklist.md` had no data-provenance or missingness-defensibility item | **fixed-P1** — `docs/provenance.md` created; checklist items added there |
| **B43** | `README_CL.md:132-208` records mask-generation commands that exit with code 2 (missing `--mar-driver-cols` under the default `strict_mar=true`) | **fixed-P1** — commands regenerated from config in the new simulator docs |
| **B44** | NHANES is missing 4 questionnaire XPT modules, losing 5 gating variables that are ideal MAR drivers | **decision-needed** (Q1-7) — assessed in T1.6e; free to download from CDC |
| **B9** | HyperImpute/TabCSDI have no MNAR results (180 runs); the exclusion is disclosed but unexplained | **pending-P2** (covered by the unified re-run) |
| **B85** ⚠️ NEW (P4-D) | The R0 bibliography asserted a publication venue that does not exist: the TabTransformer entry (Huang2020) carried "Published as a conference paper at ICLR 2021", but the paper is arXiv-only (2012.06678, v1 Dec 2020, no proceedings entry; the line is the ICLR style-file template header on circulating copies). Verified independently by Code and Chat | **fixed-P4D** — entry deleted from the Paper Y bib (it was uncited); peer-reviewed substitute if needed: Gorishniy et al., NeurIPS 2021. **Proofreading-layer item: NOT for the response letter's four categories** (those are pipeline/results defects; a single bibliographic error would dilute them) |

---

## Not carried over from R0

| Component | Reason |
|---|---|
| `SNI_v0_2/` | Already deleted in the R0 working tree; superseded by v0.3 |
| Two-layer script split (`sni/scripts/*` + `scripts/0X_*.py`) | Replaced by `code_SNI/pipelines` (P1 instruction §1, class C) |
| `outputs/figures/Fig3`–`Fig6`, `heatmap_*` | Dead artifacts — the manuscript uses none of them |
| `results_all/{tables,tables_supplement}/table_results_agg.tex` | 171-byte placeholder |
| `split_manifest_runner.py` | Dead path (B25) |
| `paper_profiles.yaml` profile system | Superseded by `configs/experiments/*.yaml` |
